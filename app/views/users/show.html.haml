.paper__bg
  = render partial: 'shared/header'
  %p 
    = @name
    さんの投稿一覧
  .posts
    - @posts.each do |post|
      = render partial: "post", locals: { post: post }
  .index_paginate= paginate(@posts)